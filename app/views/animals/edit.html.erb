<div class="row">
  <div class="small-6 columns">
    <% if @animal.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this post    from being saved:</h2>

          <ul>
          <% @animal.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
    <% end %>
    <div>
      <%= simple_form_for @animal do |a| %>
          <%= a.input :name %>
          <%= a.input :color %>
          <%= a.input :age %>
          <%= a.input :additional_notes, label: false %>
          <%= a.input :image %>
          <%= a.button :submit, "Save" %>
      <% end %>
    </div>


  </div>
</div>
<div>Current image:</div>
<div><img src="<%= @animal.image.url%>"></div>
